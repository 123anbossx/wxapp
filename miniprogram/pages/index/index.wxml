<!--index.wxml-->
 <block>
    <view bind:tap="changename">修改名字</view>
    <view wx:for="{{userDatas}}" wx:for-item="userItem" wx:key="userId">
        <text bind:tap="getData">{{userItem.userName}}</text>
    </view>
 </block>

